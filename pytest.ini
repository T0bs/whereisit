[pytest]
testpaths = tests
markers =
    committed_writes: test needs handler writes to actually commit (e.g. for MySQL FULLTEXT visibility); the conftest fixture TRUNCATEs after the test instead of wrapping in a rollback.
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
    ignore::DeprecationWarning:fastapi.*
