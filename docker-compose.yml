services:
  db:
    image: mysql:8
    environment:
      - MYSQL_ROOT_PASSWORD=rootpw
      - MYSQL_DATABASE=whereisit
      - MYSQL_USER=whereisit
      - MYSQL_PASSWORD=whereisitpw
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
volumes:
  db_data:
